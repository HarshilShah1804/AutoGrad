name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  
jobs:
  Run_all_test:
    name:  Run all python tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run tests
        run: python3 run_tests.py